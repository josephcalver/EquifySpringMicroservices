spring:
  application:
    name: dealsservice
  profiles:
    active: default
  cloud:
    config:
#      uri: http://localhost:8888
      uri: http://configserver:8888


#      uri: http://configserver:8888
#      username: root
#      password: password
#    refresh:
#      enabled: false
management:
  endpoints:
    web:
      exposure:
        include: refresh

#spring.application.name=dealsservice
#spring.cloud.config.uri=http://localhost:8888

